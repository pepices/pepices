pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo "Hello World uhjkj ,m m,jm"'
                sh '''
                    echo "Multiline shell steps works too"
                    ls -lah
                '''
            }
        }
    }
}
